---
// Story 1-2 placeholder. Real content arrives in story 1-6.
//
// ⚠️ CI TRIPWIRE: `MLW-SPIKE-1` below is asserted by the deploy workflow's
// build gate AND its live check. Story 1-6 replaces this page — when it does,
// update .github/workflows/deploy.yml in the SAME commit or the build fails
// closed. (The per-commit proof is dist/build-id.txt, which lives outside page
// content and is unaffected.)
import BaseLayout from '../layouts/BaseLayout.astro';

const MARKER = 'MLW-SPIKE-1';
const DEPLOY_STAMP = 'deploy-stamp: 002';
---

<BaseLayout
  title="Movement Lab — deploy chain spike"
  description="Staging placeholder for the Movement Lab website rebuild. Real content lands in story 1-6."
>
  <h1>Movement Lab &mdash; deploy chain spike</h1>
  <p>
    Staging only. This page is not the Movement Lab website. It exists to prove
    that a change pushed to git becomes visible on a real URL.
  </p>
  <p>Marker: <code>{MARKER}</code></p>
  <p>Stamp: <code>{DEPLOY_STAMP}</code></p>
</BaseLayout>
